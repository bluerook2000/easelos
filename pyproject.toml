[project]
name = "easelos-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cadquery>=2.7.0",
    "ezdxf>=0.18",
    "cairosvg>=2.7.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
